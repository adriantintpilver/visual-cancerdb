<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>CancerDB edit</title>
    <%- include("partials/head") -%>
</head>

<body>

    <%- include("partials/navigation") %>

    <div class="container p-4">
        <div class="row">
            <div class="col-md-4 offset-md-4">
                <div class="card">
                    <div class="card-header text-center">
                        <h3>Edit an Image</h3>
                    </div>
                    <div class="card-body">
                        <form action="/edit" method="post" enctype="multipart/form-data">
                            <div class="input-group mb-3">
                                <img src="<%= image.path %>" class="card-img-top border-25 soft-shadow" alt="<%= image.filename %>">
                                <div class="custom-file">
                                    <input type="file" name="image" class="custom-file-input" id="inputGroupFile02" value="<%= image.filename %>">
                                    <label class="custom-file-label" for="inputGroupFile02" aria-describedby="inputGroupFileAddon02">Choose file</label>
                                </div>
                            </div>
                            <div class="form-group">
                                <input type="text" name="title" placeholder="Title" class="form-control" value="<%= image.title %>">
                            </div>
                            <div class="form-group">
                                <textarea name="description" placeholder="Description" class="form-control"><%= image.description %></textarea>
                            </div>
                            <div class="form-group">
                                <input type="text" name="copyright_name" placeholder="Copyright Name" class="form-control" value="<%= image.copyright_name %>">
                            </div>
                            <div class="form-group">
                                <input type="text" name="copyright_link" placeholder="Copyright Link" class="form-control" value="<%= image.copyright_link %>">
                            </div>
                            <div class="form-group">
                                <select name="video_type" class="form-control">
                                    <option <% if (image.video_type === 1) {%>selected <% } %>value="1">YouTube</option>
                                    <option <% if (image.video_type === 2) {%>selected <% } %>value="2">Vimeo</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <input type="text" name="video_id" placeholder="video ID" class="form-control" value="<%= image.video_id %>">
                            </div>
                            <div class="form-group">
                                <button class="btn light-green btn-block">
                                    Save
                                </button>
                            </div>
                            <input type="hidden" name="id" value="<%= image._id %>">
                        </form>
                    </div>
                </div>
            </div>
        </div>

    </div>

</body>

</html>